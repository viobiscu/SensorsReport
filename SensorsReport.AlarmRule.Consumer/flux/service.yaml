apiVersion: v1
kind: Service
metadata:
  name: sensors-report-alarmrule-consumer
spec:
  selector:
    app: sensors-report-alarmrule-consumer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
