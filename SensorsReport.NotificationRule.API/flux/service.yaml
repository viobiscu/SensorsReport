apiVersion: v1
kind: Service
metadata:
  name: sensors-report-notificationrule-api
spec:
  selector:
    app: sensors-report-notificationrule-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
