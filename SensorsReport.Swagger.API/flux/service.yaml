apiVersion: v1
kind: Service
metadata:
  name: sensors-report-swagger-api
spec:
  selector:
    app: sensors-report-swagger-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
