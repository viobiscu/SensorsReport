apiVersion: v1
kind: Service
metadata:
  name: sensors-report-business-broker-api
spec:
  selector:
    app: sensors-report-business-broker-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
