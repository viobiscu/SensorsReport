apiVersion: v1
kind: Service
metadata:
  name: sensors-report-explorer-frontend
spec:
  selector:
    app: sensors-report-explorer-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
