apiVersion: v1
kind: Service
metadata:
  name: sensors-report-notificationrule-consumer
spec:
  selector:
    app: sensors-report-notificationrule-consumer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
