apiVersion: v1
kind: Service
metadata:
  name: sensors-report-provisioning-api
spec:
  selector:
    app: sensors-report-provisioning-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
