apiVersion: v1
kind: Service
metadata:
  name: sensors-report-workflow-api
spec:
  selector:
    app: sensors-report-workflow-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
